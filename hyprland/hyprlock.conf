# BACKGROUND
background {
    monitor =
    path = ~/Pictures/Backgrounds/johannes-plenio-hvrpOmuMrAI-unsplash.jpg
    blur_passes = 0
    contrast = 0.8916
    brightness = 0.8916
    vibrancy = 0.8916
    vibrancy_darkness = 0.0
}

shape {
    monitor =
    size = 610, 377
    color = rgba(255, 255, 255, 0.8)
    rounding = 32
    border_size = 0
    rotate = 0
    xray = false
    position = 0, 0
    halign = center
    valign = center
    zindex = 1
}
shape {
    monitor =
    size = 610, 377
    color = rgba(255, 255, 255, 0.4)
    rounding = 32
    border_size = 0
    rotate = 0
    xray = false
    position = -10, 10
    halign = center
    valign = center
    zindex = 1
}

# Time
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%I")</span>"
    color = rgba(39, 33, 22, 0.80)
    font_size = 100
    font_family = Fira Code Nerd Font Bold
    position = -180, 80
    halign = center
    valign = center
    zindex = 3
}
# Time
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%I")</span>"
    color = rgba(39, 33, 22, 0.40)
    font_size = 100
    font_family = Fira Code Nerd Font Bold
    position = -185, 85
    halign = center
    valign = center
    zindex = 2
}
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%M")</span>"
    color = rgba(39, 33, 22, 0.80)
    font_size = 100
    font_family = Fira Code Nerd Font Bold
    position = -140, 20
    halign = center
    valign = center
    zindex = 4

}
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%M")</span>"
    color = rgba(39, 33, 22, 0.40)
    font_size = 100
    font_family = Fira Code Nerd Font Bold
    position = -145, 25
    halign = center
    valign = center
    zindex = 5
}

# Day-Month-Date
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date  +"%A\n%B %d")"
    color = rgba(39, 33, 22, 0.80)
    font_size = 19
    font_family = Fira Code Nerd Font Bold
    position = -160, -100
    halign = center
    valign = center
    zindex = 2
}
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date  +"%A\n%B %d")"
    color = rgba(39, 33, 22, 0.40)
    font_size = 19
    font_family = Fira Code Nerd Font Bold
    position = -162, -98
    halign = center
    valign = center
    zindex = 2
}


image {
    monitor =
    path = /usr/share/pixmaps/archlinux-logo-text.png
    size = 80
    border_size = 0
    rotate = 0
    reload_time = -1
    reload_cmd = 
    position = 130, 90
    halign = center
    valign = center
    zindex = 2
}

# USER-BOX
shape {
    monitor =
    size = 280, 55
    color = rgba(39, 33, 22, 0.8)
    rounding = -1
    border_size = 0
    border_color = rgba(0, 0, 0, 0.8)
    rotate = 0
    xray = false # if true, make a "hole" in the background (rectangle of specified size, no rotation)

    position = 130, -25.5
    halign = center
    valign = center
    zindex = 2
}
shape {
    monitor =
    size = 280, 55
    color = rgba(39, 33, 22, 0.2)
    rounding = -1
    border_size = 0
    border_color = rgba(0, 0, 0, 0.8)
    rotate = 0
    xray = false # if true, make a "hole" in the background (rectangle of specified size, no rotation)

    position = 125, -20.5
    halign = center
    valign = center
    zindex = 2
}

# USER
label {
    monitor =
    text =  $USER
    color = rgba(216, 222, 233, 0.80)
    font_size = 16
    font_family = Fira Code Nerd Font Bold
    position = 130, -25.5
    halign = center
    valign = center
    zindex = 2    
}

# INPUT FIELD
input-field {
    monitor =
    size = 280, 55
    outline_thickness = 0
    dots_size = 0.2 # Scale of input-field height, 0.4 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(255, 255, 255, 0)
    inner_color = rgba(39, 33, 22, 0.8)
    font_color = rgb(200, 200, 200)
    fade_on_empty = false
    font_family = Fira Code Nerd Font Bold
    placeholder_text = <span foreground="##ffffff99">Enter Password</span>
    hide_input = false
    position = 130, -105.5
    halign = center
    valign = center
    zindex = 2    
}
shape {
    monitor =
    size = 280, 55
    color = rgba(39, 33, 22, 0.4)
    rounding = -1
    border_size = 0
    border_color = rgba(0, 0, 0, 0.8)
    rotate = 0
    xray = false # if true, make a "hole" in the background (rectangle of specified size, no rotation)

    position = 125, -100.5
    halign = center
    valign = center
    zindex = 2
}